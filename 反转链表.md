# 思路
思路一：关键在于  要设置新的链表，就要一个一个的连起来，同时保证新连的链表的下一个结点不丢失，也就是遍历原链表的时候要先单独记录当前节点的下一节点，设置哑结点，既然是反转链表，也就是头结点要变到尾，也就是依次的要被后面的结点所连接，而并非只是简单的他最后要连接一个空节点，
所以一样的思路，哑结点的下一个结点也要被记录也就是使得cur.next = dummy.next,然后再由dummy.next = cur, 这样一个新的链条就好了，   这也就是一个简单的插入的过程，然后原来的next正好也变成下一个要被插入的结点。  整体的思路就是 插入（新的链表） + 遍历。
